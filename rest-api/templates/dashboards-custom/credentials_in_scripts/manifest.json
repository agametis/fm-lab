{
  "id": "credentials_in_scripts",
  "version": "1.0.0",
  "title": "Credentials in scripts",
  "description": "Finds script steps that reference credential-related keywords (password, secret, apikey, token, …) across all files. Actual values are masked with ***** so the dashboard never leaks plaintext secrets.",
  "author": "fm-lab custom",
  "icon": "shield-alert",
  "tags": ["custom", "security", "scripts"],
  "entry": "layout.json",
  "datasets": [
    { "id": "credentials_kpi",      "source": "bundle:data/credentials_kpi.sql" },
    { "id": "credentials_findings", "source": "bundle:data/credentials_findings.sql" }
  ],
  "params": [
    { "name": "file", "type": "string", "required": false,
      "description": "Optional file filter — derived from the app-level filter." }
  ],
  "permissions": { "read_only": true, "allow_navigation": true }
}
