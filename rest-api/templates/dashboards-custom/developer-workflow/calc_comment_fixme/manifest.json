{
  "id": "calc_comment_fixme",
  "version": "1.0.0",
  "title": "FIXME marker in a calculation comment",
  "description": "Unfinished-work rule — a FileMaker calculation carries a FIXME marker (FIXME, FIX ME, FIX IT, FIXIT) inside one of its comments. Five slots are scanned: custom-function bodies, field calculations, auto-enter and validation calculations, and script step calculations. The marker only counts inside a comment segment (// to end of line, or /* … */) — a string literal \"FIX IT\" in a Substitute() is not a finding.",
  "author": "fm-lab rules",
  "icon": "flag",
  "category": "documentation",
  "tags": [
    "rule",
    "documentation",
    "calculations",
    "unfinished-work"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "warning",
    "category": "documentation",
    "rationale": "A FIXME in a calculation comment marks a formula the author knew to be wrong. Calculations are the hardest place to notice such a note — they have no step list and no debugger breakpoint, so the marker is the only signal that the result is not trusted.",
    "remediation": "Fix the formula and remove the marker, or move the note into a tracker issue and leave the comment pointing at it.",
    "meta": {
      "name": "FIXME marker in a calculation comment",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Unfinished-work rule — a FIXME marker inside a calculation comment. Detector canon of the family: (?i)\\bfix[\\s\\-_]?(it|me)\\b, verified by tools/tests/rules/unfinished_work_detector.sql, which also covers the comment-segment extraction. Code_Chunks offers no comment type (NoRef | VariableReference | FunctionRef | CustomFunctionRef | FieldRef), so the segments are extracted by regex; a // inside a string literal (a URL) is a known limit. Overlap with calc_comment_todo is intended.",
      "created": "2026-08-16",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [
      "CustomFunction",
      "Field",
      "Script"
    ],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file",
        "object",
        "object-list",
        "cluster"
      ],
      "anchor": "nav_uuid",
      "mode": {
        "summary": "native",
        "findings": "native"
      }
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — FIXME marker in a calculation comment"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  },
  "messages": {
    "custom_function": "FIXME marker in a custom-function comment",
    "field_calc": "FIXME marker in a field calculation comment",
    "field_autoenter": "FIXME marker in an auto-enter calculation comment",
    "field_validation": "FIXME marker in a validation calculation comment",
    "script_step": "FIXME marker in a script step calculation comment"
  }
}
