{
  "id": "calc_comment_todo",
  "version": "1.0.0",
  "title": "TODO marker in a calculation comment",
  "description": "Unfinished-work rule — a FileMaker calculation carries a TODO marker (TODO, TO DO, TO-DO, TBD, to be done, to be defined) inside one of its comments. Five slots are scanned: custom-function bodies, field calculations, auto-enter and validation calculations, and script step calculations. The marker only counts inside a comment segment (// to end of line, or /* … */) — a string literal \"TODO\" in a Substitute() is not a finding. A TODO describes planned work, not a defect, so the severity is info: the rule counts with its own figure but never turns the traffic light yellow. The defect half of the family is calc_comment_fixme.",
  "author": "fm-lab rules",
  "icon": "list-todo",
  "category": "documentation",
  "tags": [
    "rule",
    "documentation",
    "calculations",
    "unfinished-work"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "documentation",
    "rationale": "A TODO in a calculation comment records a formula that is knowingly incomplete — a rounding rule still to be agreed, a case not yet covered. That is a stock figure, not a defect.",
    "remediation": "Nothing to fix per finding. Use the inventory to see where formulas are still provisional, and move anything with an owner and a date into the tracker.",
    "meta": {
      "name": "TODO marker in a calculation comment",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Unfinished-work rule — a TODO marker inside a calculation comment. Detector canon of the family: (?i)(\\bto[\\s\\-_]?do|\\btbd\\b|\\bto[\\s\\-_]be[\\s\\-_](done|defined)\\b), verified by tools/tests/rules/unfinished_work_detector.sql, which also covers the comment-segment extraction; the family deliberately omits the trailing word boundary so plural markers (\"TO DOs\") still match. Code_Chunks offers no comment type (NoRef | VariableReference | FunctionRef | CustomFunctionRef | FieldRef), so the segments are extracted by regex; a // inside a string literal (a URL) is a known limit. Overlap with calc_comment_fixme is intended.",
      "created": "2026-08-16",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [
      "CustomFunction",
      "Field",
      "Script"
    ],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file",
        "object",
        "object-list",
        "cluster"
      ],
      "anchor": "nav_uuid",
      "mode": {
        "summary": "native",
        "findings": "native"
      }
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — TODO marker in a calculation comment"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  },
  "messages": {
    "custom_function": "TODO marker in a custom-function comment",
    "field_calc": "TODO marker in a field calculation comment",
    "field_autoenter": "TODO marker in an auto-enter calculation comment",
    "field_validation": "TODO marker in a validation calculation comment",
    "script_step": "TODO marker in a script step calculation comment"
  }
}
