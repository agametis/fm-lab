{
  "id": "uuid_clone_collisions",
  "version": "1.0.0",
  "title": "UUID Clone Collisions",
  "description": "Metadata-integrity check — native FileMaker UUIDs that exist in more than one file of the solution (cloned files, copy/paste between clones). Invisible inside FileMaker, but they disturb developer tooling that assumes UUID uniqueness.",
  "author": "fm-lab rules",
  "icon": "fingerprint",
  "category": "metadata_integrity",
  "tags": [
    "rule",
    "metadata",
    "uuid",
    "clones"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "warning",
    "category": "metadata_integrity",
    "rationale": "Cloned files carry the clone's UUIDs, so the same UUID identifies different objects in different files. The solution runs fine, but DDR tools, fmIDE, diff/merge tooling and any UUID-keyed integration can pick the wrong object.",
    "remediation": "No automatic fix exists. Claris' repair mode assigns entirely new UUIDs and breaks every external UUID reference. Document the collisions and make UUID-based tooling file-aware; treat the pair (UUID, file) as the object identity.",
    "meta": {
      "name": "UUID Clone Collisions",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Native FileMaker UUIDs present in more than one file of the solution.",
      "created": "2026-08-04",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file",
        "object",
        "object-list",
        "cluster"
      ],
      "anchor": "nav_uuid",
      "mode": {
        "findings": "native",
        "summary": "native"
      }
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of native UUIDs that exist in more than one file of the solution"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    },
    {
      "id": "file_pairs",
      "source": "bundle:data/file_pairs.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (occurrences in this file; pair rows involving this file)."
    },
    {
      "name": "scope_uuids",
      "type": "string",
      "required": false,
      "description": "CSV of Object_UUIDs supplied by the Analysis-Tests run boundary."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of finding rows returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
