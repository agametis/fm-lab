{
  "id": "odbc_ess_sql",
  "version": "1.0.0",
  "title": "ODBC / ESS / SQL",
  "description": "Inventory of external SQL data access: Execute SQL steps (Step_ID 117), ODBC / ESS data sources (non-FileMaker external sources) and fmxdbc (ODBC/JDBC) grants. A generic template — useful even where the corpus currently has no ODBC sources.",
  "author": "fm-lab",
  "icon": "database",
  "tags": [ "template", "modularization", "odbc", "sql" ],
  "entry": "layout.json",
  "datasets": [
    { "id": "summary", "source": "bundle:data/summary.sql" },
    { "id": "findings", "source": "bundle:data/findings.sql", "params": { "limit": 200 } },
    { "id": "ess_sources", "source": "bundle:data/ess_sources.sql", "params": { "limit": 40 } },
    { "id": "grants", "source": "bundle:data/grants.sql", "params": { "limit": 100 } }
  ],
  "params": [
    { "name": "file", "type": "string", "required": false, "description": "Optional file filter (from the app-level filter)." },
    { "name": "limit", "type": "number", "required": false, "description": "Maximum number of rows returned by the lists." }
  ],
  "permissions": { "read_only": true, "allow_navigation": true }
}
