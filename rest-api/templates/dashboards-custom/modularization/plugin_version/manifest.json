{
  "id": "plugin_version",
  "version": "1.0.0",
  "title": "MBS plugin version",
  "description": "Plugin version requirement inventory: the minimum plugin version this solution needs, derived as the maximum since_version over all plug-in functions in use (verbatim per-function introduction versions from the MBS docs mirror via reference/plugin_spec.duckdb; numeric comparison via since_version_num, never lexicographic). Per-file spread and the driver functions make the derivation verifiable. The catalog knows no installed plugin version — pick one in the header select to validate against it: the neutral entry runs no check; choosing a version flags every call to a function introduced later (installed fleets are often heterogeneous, so divergent per-workstation versions can be validated one by one). Resolution is plugin-agnostic over plugref.plugins.",
  "author": "fm-lab",
  "icon": "milestone",
  "tags": [
    "template",
    "modularization",
    "plugin",
    "mbs",
    "version",
    "maintenance"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "inventory",
    "rationale": "The required plugin version is a derivation — the maximum documented introduction version (since_version) over the plug-in functions the solution actually calls. It is a deployment requirement, not a defect.",
    "remediation": "Roll the plugin out at or above the required version on every workstation and server that executes the affected scripts. The drivers table shows which functions carry the requirement — replacing a driver can lower it."
  },
  "analysis": {
    "objectTypes": [
      "Script"
    ],
    "outputTypes": [
      "text",
      "findings-table",
      "inventory-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file",
        "object",
        "object-list",
        "cluster"
      ],
      "anchor": "nav_uuid",
      "mode": {
        "summary": "native",
        "findings": "native",
        "by_file": "native",
        "drivers": "native",
        "versions": "static"
      }
    },
    "requires": [
      "plugin-spec"
    ],
    "defaultResult": {
      "dataset": "summary",
      "column": "required_version",
      "type": "text",
      "name": "plugin_version_required",
      "meaning": "Minimum plugin version this scope requires — maximum documented introduction version over the plug-in functions in use"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "versions",
      "source": "bundle:data/versions.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    },
    {
      "id": "by_file",
      "source": "bundle:data/by_file.sql"
    },
    {
      "id": "drivers",
      "source": "bundle:data/drivers.sql",
      "params": {
        "limit": 100
      }
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "installed_version",
      "type": "string",
      "required": false,
      "sticky": true,
      "description": "Installed plugin version to validate against (major.minor, from the header select). Empty = no check: the dashboard stays a neutral inventory."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of rows returned by the lists."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
