{
  "id": "process_execution",
  "version": "1.0.0",
  "title": "Process & shell execution",
  "description": "Inventory of external process / shell execution: Send Event and Perform AppleScript (Step_ID-gated, locale-independent) plus MBS Shell / RunTask / Process calls (resolved via calls_pluginfunction). Shows where the solution hands control to the operating system.",
  "author": "fm-lab",
  "icon": "terminal",
  "tags": [
    "template",
    "modularization",
    "process",
    "shell"
  ],
  "entry": "layout.json",
  "datasets": [
    { "id": "summary", "source": "bundle:data/summary.sql" },
    { "id": "by_mechanism", "source": "bundle:data/by_mechanism.sql", "params": { "limit": 30 } },
    { "id": "findings", "source": "bundle:data/findings.sql", "params": { "limit": 400 } }
  ],
  "params": [
    { "name": "file", "type": "string", "required": false, "description": "Optional file filter (from the app-level filter)." },
    { "name": "mechanism", "type": "string", "required": false, "description": "Restricts the detail list to a mechanism (Send Event / AppleScript / MBS Shell / MBS RunTask / MBS Process). 'All' (or empty) lifts the filter." },
    { "name": "limit", "type": "number", "required": false, "description": "Maximum number of rows returned by the detail list." }
  ],
  "permissions": { "read_only": true, "allow_navigation": true }
}
