{
  "id": "field_auto_index",
  "version": "1.0.0",
  "title": "Auto-index field",
  "description": "Best-practice rule — flags the risky combination of indexing set to 'None' and 'Automatically create indexes as needed' still enabled. No index exists yet, so the first find or sort silently triggers an uninterruptible on-demand index build — a latent performance time bomb with knock-on effects on storage, backups and import speed.",
  "author": "fm-lab rules",
  "icon": "zap",
  "category": "best_practices",
  "tags": [
    "rule",
    "best_practices",
    "performance"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "warning",
    "category": "best_practices",
    "rationale": "Indexing 'None' with auto-create enabled means no index exists yet, but FileMaker builds one on-demand the first time the field is searched or sorted. On large tables that build can take minutes, cannot be cancelled, and blocks the GUI with a progress bar; the new index then quietly inflates file size, backups and import time.",
    "remediation": "Decide the indexing intent explicitly: for fields that are genuinely searched/sorted, pre-build the index (Minimal/All) during a maintenance window; for fields that never need one, turn off 'Automatically create indexes as needed' so the bomb cannot trigger.",
    "meta": {
      "name": "Auto-index field",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Flagged: fields with indexing 'None' while 'Automatically create indexes as needed' stays enabled — a risky combination, because no index exists yet and the first find or sort silently triggers an uninterruptible on-demand index build.",
      "created": "2026-07-15",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file"
      ],
      "anchor": "nav_uuid",
      "mode": {}
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Auto-index field"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
