{
  "locked": true,
  "id": "lookup_field",
  "version": "1.0.0",
  "title": "Lookup field",
  "description": "Inventory of fields populated by a Lookup. A lookup deliberately captures a value at set time (a snapshot) — often exactly what is intended (e.g. freezing a price, tax rate or address on a document). Listed for awareness, not as a defect.",
  "author": "fm-lab rules",
  "icon": "copy-plus",
  "category": "best_practices",
  "tags": [
    "rule",
    "pmd",
    "best_practices"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "best_practices",
    "rationale": "A looked-up value is captured once at set time and does not follow later changes to the source. This is frequently the intended behavior; there are good reasons to freeze a value in a solution.",
    "remediation": "No action needed in most cases. Only reconsider if the value is expected to stay in sync with its source — then a related field or calculation would keep it live.",
    "meta": {
      "name": "Lookup field",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Inventory of fields populated by a Lookup. A lookup deliberately captures a value at set time (a snapshot) — often exactly what is intended (e.g. freezing a price, tax rate or address on a document). Listed for awareness, not as a defect.",
      "created": "2026-07-01",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file"
      ],
      "anchor": "nav_uuid",
      "mode": {}
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Lookup field"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
