{
  "locked": true,
  "id": "layout_many_objects",
  "version": "1.0.0",
  "title": "Layout with many objects",
  "description": "Code-style / performance rule — layouts with many objects (threshold adjustable via the header slider, default 250). Heavy layouts are slow to render and hard to maintain.",
  "author": "fm-lab rules",
  "icon": "layout-grid",
  "category": "code_style",
  "tags": [
    "rule",
    "pmd",
    "code_style"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "code_style",
    "rationale": "Hundreds of objects on one layout slow rendering and complicate edits.",
    "remediation": "Split into multiple layouts / tab panels, or hide rarely-used parts.",
    "meta": {
      "name": "Layout with many objects",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Code-style / performance rule — layouts with many objects (threshold adjustable via the header slider, default 250). Heavy layouts are slow to render and hard to maintain.",
      "created": "2026-07-01",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file"
      ],
      "anchor": "nav_uuid",
      "mode": {}
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Layout with many objects"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    },
    {
      "name": "min_objects",
      "type": "number",
      "required": false,
      "description": "Minimum object count per layout (header slider; default 250)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
