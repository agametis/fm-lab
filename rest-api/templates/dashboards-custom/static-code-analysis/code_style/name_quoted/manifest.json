{
  "id": "name_quoted",
  "version": "1.0.0",
  "title": "Object name wrapped in quotes",
  "description": "Code-style rule — an object name starts and ends with a literal double quote, almost always because a name was pasted straight out of a calculation. The quotes become part of the name, so every name-based access has to reproduce them.\n\nBased on the fmCheckMate check \"Quoted Object Name\" — https://github.com/mrwatson-de/fmCheckMate-XSLT",
  "author": "fm-lab rules",
  "icon": "text-quote",
  "category": "code_style",
  "tags": [
    "rule",
    "fmcheckmate",
    "code_style"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "code_style",
    "rationale": "The quotes are invisible as a mistake and visible as part of the name. Anything that looks the name up — a script calling it, a query filtering on it, a developer typing it again — has to include them.",
    "remediation": "Rename the object without the surrounding quotes and update the references that relied on the quoted spelling.",
    "meta": {
      "name": "Object name wrapped in quotes",
      "author": "fm-lab rules",
      "source": "fmCheckMate-XSLT",
      "url": "https://github.com/mrwatson-de/fmCheckMate-XSLT",
      "description": "Code-style rule — an object name starts and ends with a literal double quote, almost always because a name was pasted straight out of a calculation. The quotes become part of the name, so every name-based access has to reproduce them.",
      "created": "2026-08-16",
      "references": [
        {
          "project": "fmCheckMate-XSLT",
          "rule": "XML2_fmCM_AnalyseLayout.xsl (XML2_fmCM_ReportQuotedObjectNames)",
          "url": "https://github.com/mrwatson-de/fmCheckMate-XSLT"
        }
      ]
    }
  },
  "analysis": {
    "objectTypes": [
      "Script",
      "Field",
      "Layout",
      "CustomFunction",
      "TableOccurrence",
      "BaseTable",
      "ValueList"
    ],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file",
        "object",
        "object-list",
        "cluster"
      ],
      "anchor": "nav_uuid",
      "mode": {
        "findings": "native",
        "summary": "native"
      }
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Object name wrapped in quotes"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  },
  "messages": {
    "quoted-name": "Name is wrapped in literal quotes"
  }
}
