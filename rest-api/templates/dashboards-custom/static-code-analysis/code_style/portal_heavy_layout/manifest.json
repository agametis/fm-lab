{
  "locked": true,
  "id": "portal_heavy_layout",
  "version": "1.0.0",
  "title": "Layout with many portals",
  "description": "Code-style / performance rule — layouts with more than six portals. Many portals each pull a related found set and slow the layout down. Threshold adjustable via the header slider (default 6).",
  "author": "fm-lab rules",
  "icon": "panels-top-left",
  "category": "code_style",
  "tags": [
    "rule",
    "pmd",
    "code_style"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "code_style",
    "rationale": "Each portal loads a related set on layout entry; many portals multiply that cost.",
    "remediation": "Load secondary portals on demand (tabs / popovers) instead of all at once.",
    "meta": {
      "name": "Layout with many portals",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Code-style / performance rule — layouts with more than six portals. Many portals each pull a related found set and slow the layout down. Threshold adjustable via the header slider (default 6).",
      "created": "2026-07-01",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file"
      ],
      "anchor": "nav_uuid",
      "mode": {}
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Layout with many portals"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    },
    {
      "name": "min_portals",
      "type": "number",
      "required": false,
      "description": "Minimum portals per layout (header slider; default 6)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
