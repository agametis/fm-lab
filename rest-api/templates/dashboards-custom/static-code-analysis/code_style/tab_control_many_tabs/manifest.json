{
  "locked": true,
  "id": "tab_control_many_tabs",
  "version": "1.0.0",
  "title": "Tab control with many tabs",
  "description": "Code-style rule — tab controls with more than six tab panels. Too many tabs hide content and are hard to navigate. Threshold adjustable via the header slider (default 6).",
  "author": "fm-lab rules",
  "icon": "folder-tree",
  "category": "code_style",
  "tags": [
    "rule",
    "pmd",
    "code_style"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "code_style",
    "rationale": "A tab control with many tabs buries content and signals an overloaded layout.",
    "remediation": "Split the content across layouts, or group tabs into sub-navigations.",
    "meta": {
      "name": "Tab control with many tabs",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Code-style rule — tab controls with more than six tab panels. Too many tabs hide content and are hard to navigate. Threshold adjustable via the header slider (default 6).",
      "created": "2026-07-01",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file"
      ],
      "anchor": "nav_uuid",
      "mode": {}
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Tab control with many tabs"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    },
    {
      "name": "min_tabs",
      "type": "number",
      "required": false,
      "description": "Minimum tabs per tab control (header slider; default 6)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
