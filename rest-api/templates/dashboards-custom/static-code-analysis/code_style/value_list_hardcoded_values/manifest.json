{
  "locked": true,
  "id": "value_list_hardcoded_values",
  "version": "1.0.0",
  "title": "Value list with hard-coded values",
  "description": "Code-style rule — value lists built from custom (hard-coded) values instead of a field. Hard-coded lists drift out of date and cannot be edited by users.",
  "author": "fm-lab rules",
  "icon": "list-checks",
  "category": "code_style",
  "tags": [
    "rule",
    "pmd",
    "code_style"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "code_style",
    "rationale": "Hard-coded value lists duplicate data and require a developer to change.",
    "remediation": "Base the value list on a field / table so it stays current and user-editable.",
    "meta": {
      "name": "Value list with hard-coded values",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Code-style rule — value lists built from custom (hard-coded) values instead of a field. Hard-coded lists drift out of date and cannot be edited by users.",
      "created": "2026-07-01",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file"
      ],
      "anchor": "nav_uuid",
      "mode": {}
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Value list with hard-coded values"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
