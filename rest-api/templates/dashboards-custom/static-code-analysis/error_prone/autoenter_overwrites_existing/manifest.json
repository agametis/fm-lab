{
  "id": "autoenter_overwrites_existing",
  "version": "1.0.0",
  "title": "Auto-enter calc overwrites existing value",
  "description": "Error-prone rule — fields whose auto-enter calculation replaces an existing value. This can silently clobber user edits on every change.",
  "author": "fm-lab rules",
  "icon": "pencil",
  "category": "error_prone",
  "tags": [
    "rule",
    "pmd",
    "error_prone"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "error_prone",
    "rationale": "\"Do not replace existing value\" unchecked means the calc wins over manual edits.",
    "remediation": "Confirm the overwrite is intended; otherwise re-enable \"Do not replace existing value\".",
    "meta": {
      "name": "Auto-enter calc overwrites existing value",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Error-prone rule — fields whose auto-enter calculation replaces an existing value. This can silently clobber user edits on every change.",
      "created": "2026-07-01",
      "references": []
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
