{
  "id": "show_dialog_in_loop",
  "version": "1.0.0",
  "title": "Show Custom Dialog inside Loop",
  "description": "Error-prone / UX rule — Show Custom Dialog inside a Loop pops a modal on every iteration, trapping the user in repeated prompts.",
  "author": "fm-lab rules",
  "icon": "message-square-warning",
  "category": "error_prone",
  "tags": [
    "rule",
    "pmd",
    "error_prone"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "warning",
    "category": "error_prone",
    "rationale": "A modal dialog per iteration blocks the script and overwhelms the user.",
    "remediation": "Collect the outcome and show one summary dialog after the loop.",
    "meta": {
      "name": "Show Custom Dialog inside Loop",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Error-prone / UX rule — Show Custom Dialog inside a Loop pops a modal on every iteration, trapping the user in repeated prompts.",
      "created": "2026-07-01",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [
      "Script"
    ],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file",
        "object",
        "object-list",
        "cluster"
      ],
      "anchor": "nav_uuid",
      "mode": {
        "findings": "native",
        "summary": "native"
      }
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Show Custom Dialog inside Loop"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    },
    {
      "name": "scope",
      "type": "string",
      "required": false,
      "description": "Filter switch: 'active' (default) hides disabled dialog steps; 'all' includes inactive ones."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
