{
  "id": "layout_broken_valuelist",
  "version": "1.0.0",
  "title": "Broken value list reference",
  "description": "Layout-quality rule — a layout object references a value list that no longer exists in its file. Resolution runs over the value-list ID, not the name, so renamed lists are not reported.\n\nBased on the fmCheckMate check \"Broken Value List <Missing>\" — https://github.com/mrwatson-de/fmCheckMate-XSLT",
  "author": "fm-lab rules",
  "icon": "list-x",
  "category": "layout_quality",
  "tags": [
    "rule",
    "fmcheckmate",
    "layout_quality"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "error",
    "category": "layout_quality",
    "rationale": "A control bound to a deleted value list offers no choices at runtime — the pop-up stays empty and data entry silently fails.",
    "remediation": "Bind the object to an existing value list, or recreate the missing one.",
    "meta": {
      "name": "Broken value list reference",
      "author": "fm-lab rules",
      "source": "fmCheckMate-XSLT",
      "url": "https://github.com/mrwatson-de/fmCheckMate-XSLT",
      "description": "Layout-quality rule — a layout object references a value list that no longer exists in its file. Resolution runs over the value-list ID, not the name, so renamed lists are not reported.",
      "created": "2026-08-15",
      "references": [
        {
          "project": "fmCheckMate-XSLT",
          "rule": "XML2_fmCM_AnalyseLayout.xsl (XML2_fmCM_ReportBrokenValueLists)",
          "url": "https://github.com/mrwatson-de/fmCheckMate-XSLT"
        }
      ]
    }
  },
  "analysis": {
    "objectTypes": [
      "Layout"
    ],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file",
        "object",
        "object-list",
        "cluster"
      ],
      "anchor": "nav_uuid",
      "mode": {
        "summary": "native",
        "findings": "native"
      }
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Broken value list reference"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  },
  "messages": {
    "missing-valuelist": "Value list no longer exists in this file"
  }
}
