{
  "id": "layout_popover_in_popover",
  "version": "1.0.0",
  "title": "Popover inside a popover",
  "description": "Layout-quality rule — a popover button is placed inside another popover panel. Nested popovers do not work reliably in FileMaker; opening the inner popover closes the outer one.\n\nBased on the fmCheckMate check \"Popover in popover\" — https://github.com/mrwatson-de/fmCheckMate-XSLT",
  "author": "fm-lab rules",
  "icon": "picture-in-picture",
  "category": "layout_quality",
  "tags": [
    "rule",
    "fmcheckmate",
    "layout_quality"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "error",
    "category": "layout_quality",
    "rationale": "FileMaker does not support opening a popover from inside another popover — the outer popover closes when the inner one opens, so the UI flow breaks.",
    "remediation": "Move the inner popover button outside the popover panel, or replace it with a card window or a separate panel.",
    "meta": {
      "name": "Popover inside a popover",
      "author": "fm-lab rules",
      "source": "fmCheckMate-XSLT",
      "url": "https://github.com/mrwatson-de/fmCheckMate-XSLT",
      "description": "Layout-quality rule — a popover button is placed inside another popover panel. Nested popovers do not work reliably in FileMaker; opening the inner popover closes the outer one.",
      "created": "2026-08-15",
      "references": [
        {
          "project": "fmCheckMate-XSLT",
          "rule": "XML2_fmCM_AnalyseLayout.xsl (XML2_fmCM_ReportPopoverInPopover)",
          "url": "https://github.com/mrwatson-de/fmCheckMate-XSLT"
        }
      ]
    }
  },
  "analysis": {
    "objectTypes": [
      "Layout"
    ],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file",
        "object",
        "object-list",
        "cluster"
      ],
      "anchor": "nav_uuid",
      "mode": {
        "findings": "native",
        "summary": "native"
      }
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Popover inside a popover"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  },
  "messages": {
    "nested": "Popover button is nested inside another popover panel — nested popovers close the outer popover"
  }
}
