{
  "id": "onrecordload_trigger",
  "version": "1.0.0",
  "title": "OnRecordLoad trigger",
  "description": "Performance rule — OnRecordLoad script triggers. They fire for every record scrolled into view; heavy scripts here slow down list views and finds.",
  "author": "fm-lab rules",
  "icon": "zap",
  "category": "performance",
  "tags": [
    "rule",
    "pmd",
    "performance"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "performance",
    "rationale": "OnRecordLoad runs on every record load; a slow script multiplies across the found set.",
    "remediation": "Keep the trigger script minimal, or move heavy work to an explicit user action.",
    "meta": {
      "name": "OnRecordLoad trigger",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Performance rule — OnRecordLoad script triggers. They fire for every record scrolled into view; heavy scripts here slow down list views and finds.",
      "created": "2026-07-01",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [
      "Script"
    ],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file",
        "object",
        "object-list",
        "cluster"
      ],
      "anchor": "nav_uuid",
      "mode": {
        "findings": "native",
        "summary": "native"
      }
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — OnRecordLoad trigger"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
