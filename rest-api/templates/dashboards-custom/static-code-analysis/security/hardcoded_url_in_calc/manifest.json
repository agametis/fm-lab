{
  "locked": true,
  "id": "hardcoded_url_in_calc",
  "version": "1.0.0",
  "title": "Hard-coded URL in Calculation",
  "description": "Security / maintainability rule — calculation chunks with a literal http(s):// URL. Hard-coded endpoints are brittle and leak infrastructure.",
  "author": "fm-lab rules",
  "icon": "link",
  "category": "security",
  "tags": [
    "rule",
    "pmd",
    "security"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "warning",
    "category": "security",
    "rationale": "Hard-coded URLs break when infrastructure moves and expose internal endpoints in the DDR.",
    "remediation": "Move the base URL into a settings field / global so it can change without editing calculations.",
    "meta": {
      "name": "Hard-coded URL in Calculation",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Security / maintainability rule — calculation chunks with a literal http(s):// URL. Hard-coded endpoints are brittle and leak infrastructure.",
      "created": "2026-07-01",
      "references": []
    }
  },
  "analysis": {
    "objectTypes": [],
    "outputTypes": [
      "count",
      "findings-table"
    ],
    "scope": {
      "supported": [
        "solution",
        "file"
      ],
      "anchor": "nav_uuid",
      "mode": {}
    },
    "defaultResult": {
      "dataset": "summary",
      "column": "finding_count",
      "type": "number",
      "name": "finding_count",
      "meaning": "Number of findings — Hard-coded URL in Calculation"
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
