{
  "id": "account_disabled",
  "version": "1.0.0",
  "title": "Disabled account",
  "description": "Unused-code / security rule — accounts that are disabled. Stale disabled accounts should be removed to keep the security surface clean.",
  "author": "fm-lab rules",
  "icon": "user-x",
  "category": "unused_code",
  "tags": [
    "rule",
    "pmd",
    "unused_code"
  ],
  "entry": "layout.json",
  "rule": {
    "severity": "info",
    "category": "unused_code",
    "rationale": "Disabled accounts accumulate and obscure who really has access.",
    "remediation": "Delete accounts that are no longer needed instead of leaving them disabled.",
    "meta": {
      "name": "Disabled account",
      "author": "fm-lab rules",
      "source": "fm-lab",
      "url": null,
      "description": "Unused-code / security rule — accounts that are disabled. Stale disabled accounts should be removed to keep the security surface clean.",
      "created": "2026-07-01",
      "references": []
    }
  },
  "datasets": [
    {
      "id": "summary",
      "source": "bundle:data/summary.sql"
    },
    {
      "id": "findings",
      "source": "bundle:data/findings.sql"
    }
  ],
  "params": [
    {
      "name": "file",
      "type": "string",
      "required": false,
      "description": "Optional file filter (from the app-level filter)."
    },
    {
      "name": "limit",
      "type": "number",
      "required": false,
      "description": "Maximum number of findings returned (default 500)."
    }
  ],
  "permissions": {
    "read_only": true,
    "allow_navigation": true
  }
}
